import {
  __commonJS
} from "./chunk-ICLR7M5L.js";

// node_modules/cachewrap/index.js
var require_cachewrap = __commonJS({
  "node_modules/cachewrap/index.js"(exports, module) {
    var _idGenerator = (params) => params.map((param) => param.toString()).join("-");
    var createCacheWrap = (defaultIdGenerator = _idGenerator) => {
      const cache = /* @__PURE__ */ new Map();
      const cacheWrap = (fn, idGenerator = defaultIdGenerator) => {
        const wrapped = (...params) => {
          const id = idGenerator(params);
          if (!cache.has(id)) {
            const result = fn(...params);
            if (result && result.then) {
              return new Promise((resolve) => {
                result.then((res) => {
                  cache.set(id, res);
                  resolve(res);
                });
              });
            } else
              cache.set(id, result);
          }
          return cache.get(id);
        };
        wrapped.refresh = (...params) => {
          const id = idGenerator(params);
          cache.delete(id);
          return wrapped(...params);
        };
        return wrapped;
      };
      return cacheWrap;
    };
    module.exports = createCacheWrap;
  }
});

// dep:@roxi_routify__cachewrap
var roxi_routify_cachewrap_default = require_cachewrap();
export {
  roxi_routify_cachewrap_default as default
};
//# sourceMappingURL=@roxi_routify__cachewrap.js.map
