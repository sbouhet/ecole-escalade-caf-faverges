{
  "exclude": ["node_modules", "dist", "functions"],
  "compilerOptions": {
    "checkJs": true,
    "baseUrl": "src",
    "paths": {
      "$utils/*": ["utilities/*"],
      "$components/*": ["components/*"],
      "$firestore/*": ["utilities/firebase/firestore/*"]
    }
  }
}
